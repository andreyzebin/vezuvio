settingsEvaluated {
    pluginManagement {
        repositories.clear()
        repositories {
            mavenLocal()
        }
    }
    dependencyResolutionManagement {
        repositories.clear()
        repositories {
            mavenLocal()
        }
    }
}