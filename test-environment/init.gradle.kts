settingsEvaluated {
    pluginManagement {
        repositories.clear()
        repositories {
            maven("https://jitpack.io")
        }
    }
    dependencyResolutionManagement {
        repositories.clear()
        repositories {
            maven("https://jitpack.io")
        }
    }
}